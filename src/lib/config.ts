// Re-export from data/ folder for backward compatibility
export { siteConfig } from "@/data/site"
export { steps, commitments, guarantees } from "@/data/content"
export type { Step, ContentItem, GuaranteeItem } from "@/data/site"
